version: "3.8"

services:
  product_service:
    build:
      context: "./product_service"
    ports:
      - "3001:3001"
    environment:
      - "PYTHONUNBUFFERED=1"
  customer_service:
    build:
      context: "./customer_service"
    ports:
      - "3002:3002"
    environment:
      - "PYTHONUNBUFFERED=1"
